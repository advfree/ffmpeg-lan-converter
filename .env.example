# Host directory containing media files. Relative paths are resolved from this project.
HOST_MEDIA_ROOT=./files

# Path shown and accepted by the web interface inside the container.
CONTAINER_MEDIA_ROOT=/media

TZ=Asia/Shanghai
APP_USERNAME=root

# Optional. Leave empty to generate a random password on first startup.
APP_PASSWORD=
